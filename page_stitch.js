// Author: Samuel Lewis
// Date: 7/27/2018
// This program will loop through all of the pages availabe for a specified stock and then link them together into one array and return that one array.

const parse = require('./parse');
module.exports= {
    page_stitch: function(stock_ticker, data_type)
    {
        return ;
    }
}